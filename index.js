 componentsProps={{
            footer: {
              style: {
                margin: 0, 
                fontSize: '14px'  // Adjust as needed
              }
            }
          }}
        />
      )}
